{{#with data}}

<div class="card">
	<div class="card_photo" style="background-image: url({{photo}})"></div>
	<div class="card_heading">{{heading}}</div>
	<div class="card_info">{{info}}</div>
	<a href="{{linkUrl}}" class="button card_linkText">{{linkText}}</a>
</div>

{{/with}}